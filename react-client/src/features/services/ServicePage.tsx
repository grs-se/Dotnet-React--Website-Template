import SectionContainer from "../../app/layout/SectionContainer";

export default function ServicePage() {
	return (
		<SectionContainer sectionHeader={'Community Garden'} content={
			<div className="grid">
				<h1>Content with go here</h1>
			</div>
		} />);
}